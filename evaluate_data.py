pass_theme = "bg-gradient-green-white"
warning_theme = "bg-gradient-yellow-white"
fail_theme = "bg-gradient-orange-red"


def test_effective_entropy(processed_per_base_file, selected_range_data):
    print("testing effective entropy")
    return True


def test_max_counts_non_ref(processed_per_base_file, selected_range_data):
    print("testing max counts non ref")
    return False


def test_variant_fraction(processed_per_base_file, selected_range_data):
    print("testing variant fraction")
    return False
